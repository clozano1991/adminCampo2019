$(document).ready(function(){ 
	$("#modal_borrar_elemento_contable_pago_<%=@elemento_contable_pago.id%>").modal("hide");
	//en el caso de estar en el la parte espesifica:
	$("#filaelementoContablePago_<%=@elemento_contable_pago.id%>").hide();
});